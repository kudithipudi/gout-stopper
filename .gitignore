venv/
__pycache__/
*.pyc
*.sock
data/

# Runtime logs only (see standards §7); keep the tracked .gitkeep
app/logs/*
!app/logs/.gitkeep

.env
htmlcov/
.pytest_cache/
tailwindcss

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# AI assistant / local tooling not meant for the repo.
# The graft wiring itself (.claude/, AGENTS.md, opencode.json, .mcp.json) IS
# committed so teammates share it (standards §11).
CLAUDE.md
claude.md
.ignore
.cursor/
.cursorrules
.windsurfrules
.aider*

# OS junk
.DS_Store
Thumbs.db

# coding agent config (local only)
.claude/
.claude.json
AGENTS.md
.mcp.json
opencode.json
.opencode/
.codex/
codex.md
.cursorignore
.windsurf/
GEMINI.md
.gemini/
.github/copilot-instructions.md
.copilot/
.clinerules
.cline/
.roo/
.kilocode/
.trae/
.serena/
.continue/
.codeium/

# uxcheck output (screenshots + report), regenerable.
uxcheck-report/
